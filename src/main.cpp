#include "graph.hpp"

int main() {
	// graphy::Graph graph = graphy::Graph();
	// graph.addEdge(0,1);
	// graph.addEdge(1,2);
	// graph.addEdge(2,0);
	// graph.addEdge(3,2);

	// graph.display();

	return 0;
}